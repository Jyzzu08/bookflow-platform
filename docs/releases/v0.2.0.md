# Release v0.2.0

## Compatibility

- Introduced the Fastify gateway as the single entrypoint.
- Preserved legacy `v1` endpoints while exposing the new `v2` surface.
- Stabilized the authentication and catalog compatibility flow.
